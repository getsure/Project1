import { ColorDirective } from './color.directive';

xdescribe('ColorDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ColorDirective();
  //   expect(directive).toBeTruthy();
  // });
});
